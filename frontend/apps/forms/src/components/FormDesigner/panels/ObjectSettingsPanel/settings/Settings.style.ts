import type { Styles } from "@/types/styles";

export const settingsStyle: Styles = {
  container: {
    px: 1.5,
    paddingBottom: 5,
    display: "flex",
    flexDirection: "column",
    gap: 2.5,
  },
};
